com.gitblit.servlet.GitblitContext {goSettings}, com.gitblit.GitblitServer$Params {securePort,storePassword,shutdownPort} noflow AuthTask
	where AuthTask = {c | c.message.contains("Prevent double authentication for")}